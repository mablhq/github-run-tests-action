on: [push]

name: mabl

jobs:
  test:
    name: Mabl Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Run mabl tests against deployment
        id: mabl-test-deployment
        uses: ./
        env:
          MABL_API_KEY: ${{ secrets.MABL_API_KEY }}
        with:
          application-id: l-j3VqIO6lLLOOEoFTPSxA-a
          environment-id: rtb4kWD5LuTr2KxaHvwfaA-e
        continue-on-error: true
      - name: echo
        run: echo ${{ job.steps.mabl-test-deployment.outputs.journeys_failed }}
      - name: echo outputs
        run: echo ${{ job.steps.mabl-test-deployment.outputs }}
      - name: echo steps
        run: echo ${{ job.steps }}
      - name: Do something on failure
        id: check-test-results
        run: exit 1
        if: (job.steps.mabl-test-deployment.outputs.journeys_failed > 0)
